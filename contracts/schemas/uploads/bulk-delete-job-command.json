{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BulkDeleteJobCommand",
  "description": "Command to process a bulk file-delete job.",
  "allOf": [
    {
      "$ref": "../shared/base-message.json"
    },
    {
      "type": "object",
      "properties": {
        "messageName": { "const": "BulkDeleteJobCommand" },
        "messageType": { "const": "Command" },
        "messageVersion": { "const": "1.0" },
        "publishedBy": { "const": "UploadService" },
        "consumedBy": { "const": ["UploadService"] },
        "isPublic": { "const": false }
      }
    }
  ],
  "type": "object",
  "properties": {
    "payload": {
      "type": "object",
      "description": "The payload for processing a bulk delete job.",
      "properties": {
        "jobId": {
          "type": "string",
          "description": "The unique identifier of the bulk delete job."
        }
      },
      "required": [
        "jobId"
      ]
    }
  },
  "required": [
    "payload"
  ]
}
